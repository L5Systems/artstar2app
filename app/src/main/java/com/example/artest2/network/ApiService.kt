package com.example.artest2.network

// Placeholder for network service (e.g., Retrofit interface)
interface ApiService {
    // suspend fun getData(path: String): Response<Any>
    // suspend fun postData(path: String, data: Any): Response<Any>
}

// Example of how you might create an instance (e.g., with Retrofit)
// object ApiClient {
//     val instance: ApiService by lazy {
//         Retrofit.Builder()
//             .baseUrl("https://your.api.url/")
//             .addConverterFactory(GsonConverterFactory.create())
//             .build()
//             .create(ApiService::class.java)
//     }
// }
